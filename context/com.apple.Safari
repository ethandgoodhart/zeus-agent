- Safari: "com.apple.Safari"

To search something always create a new tab and then type in the thing you need to search.
Then, wait half a second for the new page to load before executing the next task.
If there is already text in the search bar, then delete all of it before searching the user's command.